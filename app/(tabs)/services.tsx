import WebViewPage from "../../components/screen";
import { URLs } from "../../lib/constants";

const OurServicesScreen = () => {
  return <WebViewPage url={URLs.ourservices} />;
};

export default OurServicesScreen;
